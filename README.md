# Shared-Documents

#### Introduction
To run the application, you should install node.js, mongoDB and redux devtools into your browser.

#### Getting started

Run MongoDB
```
$ yarn install
$ cd client
$ yarn install
$ cd ..
$ yarn dev
```
